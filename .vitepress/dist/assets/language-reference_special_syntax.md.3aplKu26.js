import{_ as a,o as t,c as s,a2 as o}from"./chunks/framework.mRCPFc5l.js";const d=JSON.parse('{"title":"Special Syntax","description":"","frontmatter":{"title":"Special Syntax"},"headers":[],"relativePath":"language-reference/special_syntax.md","filePath":"language-reference/special_syntax.md"}'),n={name:"language-reference/special_syntax.md"};function i(r,e,l,c,h,p){return t(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="special-syntax" tabindex="-1">Special Syntax <a class="header-anchor" href="#special-syntax" aria-label="Permalink to &quot;Special Syntax&quot;">​</a></h1><p>Some modeling languages work effectively like programming languages. The code compiles to series of instructions that the computer executes, running a simulation and returning a result.</p><p>Fault compiles to <em>rules</em> and asks an SMT to produce a solution that satisfies all those rules. It then interprets that solution to look like a simulation. Fault will render rules to cover every conceivable possibility in the model, in effect running <em>every</em> possible simulation at once looking for the first one that satisfies all the rules.</p><p>When you have no assertions Fault will return the first scenario that it finds. When you declare assertions, Fault will look for a scenario where your assertions are wrong (model checking). When you declare assumptions, Fault will restrict the state graph to only scenarios where though assumptions are true.</p><p>Fault has some special syntax in order to specify other aspects of how those rules should be build</p><h2 id="the-pipe-operator" tabindex="-1">The Pipe &#39;|&#39; Operator <a class="header-anchor" href="#the-pipe-operator" aria-label="Permalink to &quot;The Pipe &#39;|&#39; Operator&quot;">​</a></h2><p>The pipe operator is used to tell Fault that these steps are happening in parallel. It will give you some feedback on concurrency. When Fault sees <code>funcA | funcB</code>, what it will do is generate two paths for the solver to choose between: one in which funcA finishes before funcB and one in which funcB finishes before funcA. The less the functions themselves do, the better this can model actual concurrency.</p><p>You can chain pipe operators ( <code>funcA | funcB | funcC</code> ) but that will cause your state space to explode. Fault will generate rules for every possible order of functions. If the functions themselves generate a lot of rules or the model has many rounds, performance on the solver may suffer.</p><h2 id="choose-ors" tabindex="-1">Choose Ors <a class="header-anchor" href="#choose-ors" aria-label="Permalink to &quot;Choose Ors&quot;">​</a></h2><p>The keyword <code>choose</code> helps clarify what Fault should do when it comes across an <code>||</code> in the state graph. You cannot use <code>choose</code> within asserts or assumptions.</p><p>In logic, the truth table for OR will produce a TRUE when one <em>or more</em> values is TRUE. This creates a scenario where instead of going down branch A or branch B, the model might go down <em>both</em>. In most cases, that isn&#39;t what you want.</p><p><code>choose A || B;</code> is close to an XOR, except when you give XOR multiple variables it will return TRUE when an odd number of them are TRUE. Again that creates cases where the model might go down multiple paths when what we want it to do is choose one.</p><p>The <code>choose</code> keyword makes that instruction explicit. <code>choose A || B || C;</code> translates to <code>(A &amp;&amp; !B &amp;&amp; !C) || (!A &amp;&amp; B &amp;&amp; !C) || (!A &amp;&amp; !B &amp;&amp; C)</code> thereby allowing only one path to be chosen.</p><h2 id="when-then" tabindex="-1">When/Then <a class="header-anchor" href="#when-then" aria-label="Permalink to &quot;When/Then&quot;">​</a></h2><p>The construction <code>when A then B</code> can only be used with asserts and assumptions to create rules that require B to always be true when A is true. B can be false if A is false, but B <em>must</em> be true if A is true.</p><h2 id="multiple-flow-instances" tabindex="-1">Multiple Flow Instances <a class="header-anchor" href="#multiple-flow-instances" aria-label="Permalink to &quot;Multiple Flow Instances&quot;">​</a></h2><p>The <code>multiple</code> keyword creates an unbounded set of flow instances rather than a single named one. The solver decides how many instances to use.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>run init {</span></span>
<span class="line"><span>    l = multiple faucet;</span></span>
<span class="line"><span>} {</span></span>
<span class="line"><span>    l.in;</span></span>
<span class="line"><span>    l.in;</span></span>
<span class="line"><span>};</span></span></code></pre></div><p><code>multiple</code> is only valid for flows, not stocks.</p><h3 id="characteristic-access" tabindex="-1">Characteristic Access: <code>::</code> <a class="header-anchor" href="#characteristic-access" aria-label="Permalink to &quot;Characteristic Access: \`::\`&quot;">​</a></h3><p>Use the <code>::</code> operator to access aggregate properties of a <code>multiple</code> set. Currently the only supported characteristic is <code>count</code> — the number of instances the solver created.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>assume l::count &lt; 5;</span></span></code></pre></div><p>This restricts the solver to solutions where fewer than five instances of <code>faucet</code> were created. Without this constraint the solver may create an arbitrarily large number of instances to satisfy other invariants.</p>`,23)])])}const m=a(n,[["render",i]]);export{d as __pageData,m as default};
