import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.mRCPFc5l.js";const m=JSON.parse('{"title":"Model Types","description":"","frontmatter":{"title":"Model Types"},"headers":[],"relativePath":"model-types/index.md","filePath":"model-types/index.md"}'),n={name:"model-types/index.md"};function i(l,e,r,d,h,c){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="model-types" tabindex="-1">Model Types <a class="header-anchor" href="#model-types" aria-label="Permalink to &quot;Model Types&quot;">​</a></h1><p>Fault supports four distinct modeling styles. Each is suited to a different kind of problem.</p><table tabindex="0"><thead><tr><th>Model Type</th><th>File Type</th><th>Use When</th></tr></thead><tbody><tr><td><a href="./stock-flow.html">Stock-Flow</a></td><td><code>.fspec</code></td><td>Modeling resource flows, queues, capacity limits</td></tr><tr><td><a href="./component-state-machines.html">Component State Machines</a></td><td><code>.fsystem</code></td><td>Modeling distributed systems, protocols, component interactions</td></tr><tr><td><a href="./program-synthesis.html">Program Synthesis</a></td><td><code>.fspec</code></td><td>Finding sequences of operations that satisfy a property</td></tr><tr><td><a href="./boolean-logic.html">Boolean Logic</a></td><td><code>.fspec</code></td><td>Pure logical modeling without numeric quantities</td></tr></tbody></table><p>If you are new to Fault, start with the model type that most closely matches the system you want to model.</p><h2 id="how-fault-models-work" tabindex="-1">How Fault Models Work <a class="header-anchor" href="#how-fault-models-work" aria-label="Permalink to &quot;How Fault Models Work&quot;">​</a></h2><h3 id="bounded-checking" tabindex="-1">Bounded Checking <a class="header-anchor" href="#bounded-checking" aria-label="Permalink to &quot;Bounded Checking&quot;">​</a></h3><p>Like most model checkers, Fault will not run your model indefinitely. It is <strong>bounded</strong>, meaning it will only check the exact number of steps you tell it to. Each line in the run block is one round of execution. Fault verifies that your assertions hold across every step.</p><p>This isn&#39;t as serious a limitation as you might assume. The trick is not to define a starting state for the variables in the model. If they are unknown, then the model checker will jump to whatever values will lead to a solution.</p><p>More steps mean larger SMT formulas, which take longer to solve. Start with 3–5 steps and increase only if you have reason to believe the failure requires more. See <a href="./../language-reference/time.html">Time and Rounds</a> for details.</p><h3 id="strings-as-booleans" tabindex="-1">Strings as Booleans <a class="header-anchor" href="#strings-as-booleans" aria-label="Permalink to &quot;Strings as Booleans&quot;">​</a></h3><p>SMT does not have string operations, and no need for them either. But in Fault will allow you to define something that looks like the string</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>a = &quot;Socrates is a man&quot;;</span></span>
<span class="line"><span>b = &quot;All men are mortal&quot;;</span></span>
<span class="line"><span>c = &quot;Socrates is mortal&quot;;</span></span></code></pre></div><p>And then treat it like a boolean!</p><p>Under the hood, SMT encodes information in variable names that look like variable names in any other language. This can make it hard to make sense of your results when you have hundreds of rules. You have to remember how each one maps to a variable name.</p><p>Fault treats strings like booleans and encodes them as SMT rules, but keeps the string itself as a docstring and reuses it when formating the results from the solver.</p><h3 id="in-flows-and-out-flows" tabindex="-1">In-flows (&lt;-) and Out-flows (-&gt;) <a class="header-anchor" href="#in-flows-and-out-flows" aria-label="Permalink to &quot;In-flows (&lt;-) and Out-flows (-&gt;)&quot;">​</a></h3><p>Fault is for programmers, not mathematicians. The syntax is designed to be familiar to programmers (without being too close ... after all, Fault is not a programming language!) and mathematical symbols common in logic are either avoided or not used.</p><p>So <code>x -&gt; y</code> is <em>not</em> the logic symbol for implies in Fault (sorry ... but also, not sorry) Instead it&#39;s just some syntaxic sugar for <code>x = x - y</code></p><p><code>x -&gt; y</code> decrements x by y. <code>x &lt;- y</code> increments x by y. Of course if this bother you, you can always just write <code>x = x - y</code> instead</p><h3 id="extending-and-multiplying-constructs" tabindex="-1">Extending and Multiplying Constructs <a class="header-anchor" href="#extending-and-multiplying-constructs" aria-label="Permalink to &quot;Extending and Multiplying Constructs&quot;">​</a></h3><p>While stocks and flows were originally named to match the format of system dynamics models, they&#39;ve developed into their own concepts within the language.</p><p>Stocks can be extended to create parent-child hierarchies. This means you can write assertions against the parent that apply to all children, or target a specific child when a constraint only belongs there.</p><p>Flows can be initiated as multiples, meaning the same flow can be executing as many concurrent instances as the solver needs to find a solution — or a failure. Rather than specifying how many, you let the solver decide, and use <code>::count</code> to bound what it&#39;s allowed to try.</p>`,23)])])}const p=t(n,[["render",i]]);export{m as __pageData,p as default};
