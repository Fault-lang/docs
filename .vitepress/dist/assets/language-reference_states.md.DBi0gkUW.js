import{_ as a,o as t,c as s,a2 as n}from"./chunks/framework.mRCPFc5l.js";const u=JSON.parse('{"title":"States","description":"","frontmatter":{"title":"States"},"headers":[],"relativePath":"language-reference/states.md","filePath":"language-reference/states.md"}'),o={name:"language-reference/states.md"};function i(c,e,r,l,p,d){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="states" tabindex="-1">States <a class="header-anchor" href="#states" aria-label="Permalink to &quot;States&quot;">​</a></h1><p>Once we have components with states we need to define what happens when a component is in that state, how it transitions out and to which state it transitions. In Fault this means a few different things:</p><ol><li>State triggers a flow</li><li>State looks up a stock value</li><li>State advances to another state</li><li>Nothing happens</li></ol><p>In other words, we use the state functions in Fault to define <strong>Mealy</strong> or <strong>Moore</strong> transitions. If you are a little rusty on state machines the 30 second summary is:</p><p>| :---- | :----------------| | <strong>Moore Machines</strong> | State -&gt; State | | <strong>Mealy Machines</strong> | State + input -&gt; State |</p><p>Let&#39;s look at the <code>idle</code> state in our cache as an example</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>idle: sfunc{</span></span>
<span class="line"><span>        advance(this.expired) || advance(this.lookupRecord);</span></span>
<span class="line"><span>    },</span></span></code></pre></div><p>From an idle state our cache does nothing and it can either advance out of the idle state by expiring records or when a request comes in, handling that request.</p><p>Notice there is no logic around which one of those paths the model should take. The <code>||</code> operator here tells Fault to choose whichever option makes sense in that scenario, which is something solvers are really good at doing. If we have asserts defined Fault will chose whichever path will result in a failure case. Since both paths are possible we don&#39;t need to simulate sending requests or define how often our regular job expiring records runs.</p><p>The <code>idle</code> function for our container manager, however, looks different</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>idle: sfunc{</span></span>
<span class="line"><span>        stay();</span></span>
<span class="line"><span>    },</span></span></code></pre></div><p><code>stay()</code> means what you think it does. Do nothing. That&#39;s because what triggers a state change out of idle for the container manager is the behavior of the cache. Without the cache the container manager will stay idle forever.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createRecord:sfunc{</span></span>
<span class="line"><span>    advance(containerMng.pullContainer);</span></span>
<span class="line"><span>    },</span></span></code></pre></div><p><code>advance()</code> can change the state of either the component it is in (notice the <code>this</code> keyword in our first example) or other components defined in the system.</p><h2 id="state-transition-builtins" tabindex="-1">State Transition Builtins <a class="header-anchor" href="#state-transition-builtins" aria-label="Permalink to &quot;State Transition Builtins&quot;">​</a></h2><p>There are three builtins for controlling state transitions:</p><table tabindex="0"><thead><tr><th>Builtin</th><th>Usage</th><th>Description</th></tr></thead><tbody><tr><td><code>advance(target)</code></td><td><code>advance(this.idle)</code></td><td>Transition to the specified state on the next loop</td></tr><tr><td><code>stay()</code></td><td><code>stay()</code></td><td>Remain in the current state</td></tr><tr><td><code>leave()</code></td><td><code>leave(this.idle)</code> or <code>leave()</code></td><td>Exit the current state; opposite of advance</td></tr></tbody></table><h2 id="choosing-between-transitions" tabindex="-1">Choosing Between Transitions <a class="header-anchor" href="#choosing-between-transitions" aria-label="Permalink to &quot;Choosing Between Transitions&quot;">​</a></h2><p>When multiple <code>advance()</code> or <code>leave()</code> calls are possible, you can use the <code>choose</code> keyword to make the non-deterministic choice explicit:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>idle: sfunc{</span></span>
<span class="line"><span>    choose advance(this.expired) || advance(this.lookupRecord);</span></span>
<span class="line"><span>},</span></span></code></pre></div><p><code>choose</code> is optional — the <code>||</code> operator already signals non-deterministic choice — but it can make the intent clearer.</p><h2 id="omitting-stay" tabindex="-1">Omitting Stay <a class="header-anchor" href="#omitting-stay" aria-label="Permalink to &quot;Omitting Stay&quot;">​</a></h2><p>Strictly speaking <code>stay()</code> isn&#39;t actually necessary. If you omit it and there are no options to transition out of the current state, you&#39;ve effectively accomplished the same thing.</p><p>But it can be nice to use <code>stay()</code> just to make sure the behavior is clear to any non-compilers reading the model. My preference is to omit stay when the state function body is already complex but to include in simple ones.</p><h2 id="interacting-with-stocks-and-flows" tabindex="-1">Interacting with Stocks and Flows <a class="header-anchor" href="#interacting-with-stocks-and-flows" aria-label="Permalink to &quot;Interacting with Stocks and Flows&quot;">​</a></h2><p>You can build an entire model just with components and states! Those are Moore Machines and they are good for things like reachability analysis where your primary concern is <em>can I get to that state from this state?</em> and you are less concerned with the exact details of how, just that there is a path available.</p><p>Mealy Machines, on the other hand, factor input into the decision whether to transition from one state to another. Perhaps a component can only transition from state A to state B if a value X is set to true.</p><p>This is where we bring in <code>.fspec</code> files. We need to be able to define under what conditions value X is set to true in order to define the state transition.</p><p>In Fault this means representing this value as a stock and having the state machine trigger flows that alter the value of that stock.</p><h3 id="importing-stocks-and-flows" tabindex="-1">Importing Stocks and Flows <a class="header-anchor" href="#importing-stocks-and-flows" aria-label="Permalink to &quot;Importing Stocks and Flows&quot;">​</a></h3><p>First thing we need to do to specify our state transitions with stocks and flows is import them.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>import(</span></span>
<span class="line"><span>        &quot;cache.fspec&quot;</span></span>
<span class="line"><span>        &quot;orchestrator.fspec&quot;</span></span>
<span class="line"><span>    );</span></span>
<span class="line"><span></span></span>
<span class="line"><span>global record = new cache.record;</span></span>
<span class="line"><span>global manager = new orchestrator.control;</span></span></code></pre></div><p>Most of the time it is only necessary to initialize a new instance of the flow, as flows will automatically import and initialize any stocks they are connected to.</p><h3 id="triggering-flows" tabindex="-1">Triggering Flows <a class="header-anchor" href="#triggering-flows" aria-label="Permalink to &quot;Triggering Flows&quot;">​</a></h3><p>For this model we&#39;ve decided we want to consider how resource utilization affects the behavior of the state machine. So whenever a request comes in, that request is going to change our stocks of resources somehow. Therefore our components being in particular states will trigger flows.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lookupRecord: sfunc{</span></span>
<span class="line"><span>        record.lookup;</span></span>
<span class="line"><span>        advance(this.returnRecord) || advance(this.createRecord);</span></span>
<span class="line"><span>    },</span></span>
<span class="line"><span>    returnRecord: sfunc{</span></span>
<span class="line"><span>        record.release;</span></span>
<span class="line"><span>        advance(this.idle);</span></span>
<span class="line"><span>    },</span></span></code></pre></div><h3 id="consulting-stocks" tabindex="-1">Consulting Stocks <a class="header-anchor" href="#consulting-stocks" aria-label="Permalink to &quot;Consulting Stocks&quot;">​</a></h3><p>While we don&#39;t see it in this model, we may want to use a stocks value to determine whether a certain state transition happens or not. We can use stocks in various conditionals. For example we may want to only create a container if we have space to create a record for it in our cache</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createRecord:sfunc{</span></span>
<span class="line"><span>     if record.machine.block &lt; 5 {</span></span>
<span class="line"><span>         advance(containerMng.pullContainer);</span></span>
<span class="line"><span>     }</span></span>
<span class="line"><span>    },</span></span></code></pre></div><h2 id="states-as-booleans" tabindex="-1">States as Booleans <a class="header-anchor" href="#states-as-booleans" aria-label="Permalink to &quot;States as Booleans&quot;">​</a></h2><p>States are written and executed as functions, but they can also be referenced as booleans in other states and even other components.</p><p>For example if I want to know if the container manager is currently shutting down a container before I try to expire old records from my cache, I can do this</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>expired: sfunc{</span></span>
<span class="line"><span>        if !containerMng.shutdownContainer{</span></span>
<span class="line"><span>            record.expire;</span></span>
<span class="line"><span>            advance(this.idle);</span></span>
<span class="line"><span>            }</span></span>
<span class="line"><span>    },</span></span></code></pre></div><p>If the <code>shutdownContainer</code> state is active <code>containerMng.shutdownContainer</code> will evaluate to true. If it&#39;s inactive it will evaluate to false.</p>`,44)])])}const g=a(o,[["render",i]]);export{u as __pageData,g as default};
