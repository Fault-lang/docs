import{_ as a,o as n,c as s,a2 as t}from"./chunks/framework.mRCPFc5l.js";const u=JSON.parse('{"title":"Time","description":"","frontmatter":{"title":"Time"},"headers":[],"relativePath":"language-reference/time.md","filePath":"language-reference/time.md"}'),i={name:"language-reference/time.md"};function r(o,e,l,c,p,d){return n(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="time" tabindex="-1">Time <a class="header-anchor" href="#time" aria-label="Permalink to &quot;Time&quot;">​</a></h1><p>Fault verifies system behavior over a bounded number of discrete rounds. A round is the fundamental unit of time — it represents one step of the system&#39;s evolution. The SMT solver checks that all assertions hold across every round.</p><p>The number of rounds is determined by how many steps you write in the run block — each line is one round.</p><h2 id="flow-based-time" tabindex="-1">Flow-Based Time <a class="header-anchor" href="#flow-based-time" aria-label="Permalink to &quot;Flow-Based Time&quot;">​</a></h2><p>In a <code>.fspec</code> file, flows execute their functions once per round. Two operators control ordering within a round:</p><h3 id="sequential-separate-lines-—-fixed-order-deterministic" tabindex="-1">Sequential (separate lines) — fixed order, deterministic: <a class="header-anchor" href="#sequential-separate-lines-—-fixed-order-deterministic" aria-label="Permalink to &quot;Sequential (separate lines) — fixed order, deterministic:&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>run {</span></span>
<span class="line"><span>       arr.enqueue;</span></span>
<span class="line"><span>       proc.drain;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>arr.enqueue always completes before proc.drain in every round.</p><h3 id="concurrent-—-non-deterministic-ordering" tabindex="-1">Concurrent (|) — non-deterministic ordering: <a class="header-anchor" href="#concurrent-—-non-deterministic-ordering" aria-label="Permalink to &quot;Concurrent (|) — non-deterministic ordering:&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>arr.enqueue | proc.drain;</span></span></code></pre></div><p>The SMT solver considers both possible orderings — enqueue first, or drain first — and verifies that assertions hold under either.</p><p>This is not parallel execution; it is a claim that the ordering is unspecified and the system must be correct regardless.</p><h2 id="state-machine-time" tabindex="-1">State Machine Time <a class="header-anchor" href="#state-machine-time" aria-label="Permalink to &quot;State Machine Time&quot;">​</a></h2><p>In a <code>.fsystem</code> file, components are state machines. Each state has a boolean variable indicating whether it is currently active. States are inactive by default; the run block sets which state each component begins in.</p><p>Both stay() and advance() are non-deterministic possibilities, not commands. A state function may contain both — the SMT solver explores all valid paths simultaneously.</p><h3 id="chains-within-a-round" tabindex="-1">Chains within a round <a class="header-anchor" href="#chains-within-a-round" aria-label="Permalink to &quot;Chains within a round&quot;">​</a></h3><p>Because advance() updates state booleans immediately, a state machine can pass through multiple states in a single round. If state A calls advance(B), B&#39;s boolean becomes true, and calls that state function. This continues until a stay() is reached or there are no more states to evaluate. State machines advance until they stabilize, not one step per round.</p><h2 id="using-flows-and-state-machines-together" tabindex="-1">Using Flows and State Machines Together <a class="header-anchor" href="#using-flows-and-state-machines-together" aria-label="Permalink to &quot;Using Flows and State Machines Together&quot;">​</a></h2><p>A run block in a <code>.fsystem</code> file specifies the ordering relationship between components and how many rounds to check:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>run {</span></span>
<span class="line"><span>    Arrival | Processor;</span></span>
<span class="line"><span>    Queue;</span></span>
<span class="line"><span>    Arrival | Processor;</span></span>
<span class="line"><span>    Queue;</span></span>
<span class="line"><span>    Arrival | Processor;</span></span>
<span class="line"><span>    Queue;</span></span>
<span class="line"><span>    Arrival | Processor;</span></span>
<span class="line"><span>    Queue;</span></span>
<span class="line"><span>    Arrival | Processor;</span></span>
<span class="line"><span>    Queue;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>This runs 10 rounds — in odd rounds, the SMT solver considers both orderings of Arrival and Processor (mirroring <code>|</code> semantics from fspec); in even rounds, Queue runs in a fixed order. Without the run block, the state machine will execute each component in the order they are defined in the specification.</p><p>Flow functions called from component state functions execute in the context of the current round. Flow variables reflect the values established by flows that ran earlier in that round — a state mid-chain observes whatever snapshot of the flow variables exists at that point in the round&#39;s SSA sequence.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The number of steps in your run block determines how deep the verification goes. Most properties are provable in 3–4 rounds, especially if you avoid declaring variables with specific values. More steps produce larger SMT formulas, which may cause performance issues or even models the solver never returns a solution for.</p><p>State changes are treated as if they happen instantaneously, each component is executed in the order it is defined, unless an order is specified in the run block. Fault will execute all the state transitions triggered by that component until it has doubled-back on a state it has seen before (to prevent infinite loops) or it has run out of state transitions.</p><p>Fault ignores the run block of imported files.</p>`,26)])])}const m=a(i,[["render",r]]);export{u as __pageData,m as default};
